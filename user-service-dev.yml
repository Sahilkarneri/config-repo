spring:
  datasource:
    url: jdbc:oracle:thin:@//10.178.224.125:1521/XEPDB1
    username: TESTT
    password: 123456
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true

stockCountry:
  portalOrganize: 8304

springdoc:
  api-docs:
    path: /v3/api-docs

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost
    prefer-ip-address: true

greeting:
  message: "b2b-user-dev development environment updated"
